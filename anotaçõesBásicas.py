"""
!= diferente em python
exponenciação	**
parte inteira	//
módulo  	%

adição	+
subtração	-
multiplicação	*
divisão	         	/

formatação
(f "      {nome da variavel }")
(\n) pula uma linha
criando indice ( cores = ['verde','azul','preto',]

for indice,cor in enumerate (cores) :
print (indice, cor)
)
.sort ordena elementos em uma lista - crescente
.count ocorrências de um valor em uma lista
.append acrescenta 1 iten por vez
.extend acrescente vários inclusive outra lista dentro
.insert adiciona em qualquer posição dentro da lista
.reverse inverte a lista
.copy copia elementos
.pop remove e retorna o ultimo elemento, também remove pelo índice
.clear limpa a lista remove tudo
.split transforma string em lista, definindo cada elemento separado por espaço entre eles
. join transforma lista em string
.index aponta o indice do elemento especificado
.update interessante para adicionar chaves/valores em dict
print(sum(lista) = somara todos valores da lista
max = valor máximo da lista
min = menor valor
len = tamanho da lista
lista = [1,2,4]
tuple(lista) = vira uma tupla
"""